[pytest]
asyncio_mode = auto
markers =
    slow: marks tests as slow (requires real API keys, deselect with -m "not slow")
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning:starlette.*
    ignore::DeprecationWarning:httpx.*
